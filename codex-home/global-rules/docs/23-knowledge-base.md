# 知识沉淀

- 沉淀可复用且会改变判断的事实、决策、操作与易错点，不抄聊天流水账。
- 项目约定进入项目规则；面向使用者的操作与接口进入 README 或 docs；跨项目核心偏好才进入全局默认。
- 修改既有权威条目，合并重复并处理失效内容；用绝对日期记录事件，区分历史与当前状态。
- 只同步受影响的上下游文档，不因出现新能力就机械创建多份文档。
- 记忆是独立的生成状态。只在用户明确要求时按运行环境指定的写入入口提交更新；不直接重写 Codex 生成的摘要或索引。
