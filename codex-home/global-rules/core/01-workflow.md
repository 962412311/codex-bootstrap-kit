# 执行流程

1. 确定目标、相关现状、约束与成功证据；只读受影响范围的规则和资料。
2. 直接完成最小闭环。复杂任务可拆阶段；小改动无需额外 spec、plan 或审批流程。
3. 验证与风险相称，失败时定位原因并从必要阶段重试。
4. 汇报结果、证据与未完成项；影响既有使用方式时同步相关文档。

多阶段工程操作把构建、安装、启动、验活分别判断；详细日志落文件，摘要保留状态与关键证据。

委派以当前会话授权和运行环境为准。仅拆分能独立推进的明确子任务；模型默认继承，不固定过期型号。

规则缺失时按已有约定和合理判断推进；只在缺失信息影响结果时澄清，不为执行任务先创造一套规范。
