# 部署与验活

- 从项目规则、脚本或 CI 确认目标环境、命令、依赖与回滚方式；不猜测部署入口。
- 区分本机、编译主机与运行设备，分别核实产物、资源和配置。
- 构建、传输或安装、启动、业务验活分别收集证据；`git push` 仅在项目明确配置时才触发部署。
- 高风险变更先准备可用回滚路径；失败后从必要阶段恢复，避免盲目重复整条流程。
- 直接 SSH 默认 `ssh user@host`；只在环境确有需要时增加选项，不习惯性关闭主机身份检查。
